# PyAlex Environment Configuration
# Copy this file to .env and customize the values

# Required: Your email address for OpenAlex API (polite pool access)
OPENALEX_EMAIL=your.email@example.com

# Optional: API key for authenticated access (higher rate limits)
# OPENALEX_API_KEY=your_api_key_here

# Optional: Custom user agent string
# OPENALEX_USER_AGENT=my-app/1.0

# Optional: OpenAlex API base URL (for testing or custom instances)
# OPENALEX_URL=https://api.openalex.org

# Optional: HTTP retry configuration
# OPENALEX_MAX_RETRIES=3
# OPENALEX_RETRY_BACKOFF=0.5

# Optional: Rate limiting configuration
# OPENALEX_RATE_LIMIT=10.0          # Requests per second (default: 10)
# OPENALEX_REQUESTS_PER_DAY=100000  # Daily request limit
# OPENALEX_RATE_BUFFER=0.9          # Use 90% of rate limit (safety margin)

# Optional: CLI configuration
# OPENALEX_CLI_BATCH_SIZE=100       # Batch size for CLI operations
# OPENALEX_CLI_MAX_WIDTH=150        # Maximum table width
# OPENALEX_CLI_NAME_LENGTH=50       # Truncate names to this length

# Optional: HTTP client timeouts (in seconds)
# OPENALEX_TOTAL_TIMEOUT=30.0
# OPENALEX_CONNECT_TIMEOUT=10.0

# Optional: HTTP connection pooling
# OPENALEX_CONNECTION_LIMIT=20
# OPENALEX_CONNECTION_LIMIT_PER_HOST=10
# OPENALEX_MAX_CONCURRENT=10
